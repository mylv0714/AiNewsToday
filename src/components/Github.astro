---
import type { BriefItem } from '../lib/schema'
import BriefItem from './BriefItem.astro'

interface Props {
  items: BriefItem[]
  editionDate: string
}

const { items, editionDate } = Astro.props
---

{
  items.length > 0 && (
    <section class="section" aria-labelledby="github-label">
      <h2 class="section-label" id="github-label">GitHub</h2>
      {items.map((item) => (
        <BriefItem item={item} editionDate={editionDate} variant="repo" />
      ))}
    </section>
  )
}
